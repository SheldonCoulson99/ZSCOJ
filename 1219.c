#include<stdio.h>
#include<string.h>
int main()
{
	int n, l;
	char a[1000] = { 0 };
	scanf("%d", &n);
	while (n--)
	{
		gets(a);
		l = strlen;
		for (int i = 0; i < l; i++)
		{

		}
	}
}