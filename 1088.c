#include<stdio.h>
int main()
{
	int t, i, j, k;
	int n, m;
	int a[1000];

	scanf("%d", &t);
	for ( i = 0; i < t; i++)
	{
		scanf("%d%d", &n, &m);
		for ( j = 0; j < n; j++)
		{
			for ( k = 0; k < m; k ++)
			{
				a[j];
			}
		}
	}
}